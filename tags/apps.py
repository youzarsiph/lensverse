""" AppConf for lensverse.tags """

from django.apps import AppConfig


# Create your AppConf here.
class TagsConfig(AppConfig):
    """App Configuration for lensverse.tags"""

    name = "lensverse.tags"
    default_auto_field = "django.db.models.BigAutoField"
