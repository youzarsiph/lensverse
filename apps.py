""" AppConf for lensverse """

from django.apps import AppConfig


# Create your AppConf here.
class LensVerseConfig(AppConfig):
    """App configuration for lensverse"""

    name = "lensverse"
    default_auto_field = "django.db.models.BigAutoField"
